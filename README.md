![编译过程](https://github.com/user-attachments/assets/4f6dd53a-3d77-40e7-ad3a-d409d37b864f
)# nxufyy
静态库是编译时直接嵌入可执行文件的库文件（后缀通常为`.a`），优点是程序运行时不依赖外部文件，缺点是会增大可执行文件体积
动态库是程序运行时才加载的库文件（后缀通常为 .so ），优点是可被多个程序共享、减小文件体积，缺点是运行时需确保库文件存在。
环境变量用于指定系统的运行环境配置（如动态库的搜索路径）。
one
