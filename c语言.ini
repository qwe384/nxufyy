#include <stdio.h>

int main() {
    int sum = 0;  // 用于存储累加结果
    
    // 循环计算1到10的和
    for (int i = 1; i <= 10; i++) {
        sum += i;
        // 打印每一步的累加过程
        printf("累加 %d 后，总和为: %d\n", i, sum);
    }
    
    // 输出最终结果
    printf("\n1到10的总和是: %d\n", sum);
    
    return 0;
}